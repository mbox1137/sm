#define DEBUG 0							#define DEBUG 0

#include <signal.h>						#include <signal.h>
#include <sys/types.h>						#include <sys/types.h>
#include <sys/wait.h>						#include <sys/wait.h>
#include <stdio.h>						#include <stdio.h>
#include <stdlib.h>						#include <stdlib.h>
#include <unistd.h>						#include <unistd.h>
#include <string.h>						#include <string.h>
#include <malloc.h>						#include <malloc.h>
#include <fcntl.h>						#include <fcntl.h>

int startArithmeticProgression(int h, int i, int n, int a0, i	int startArithmeticProgression(int h, int i, int n, int a0, i
    pid_t cpid;							    pid_t cpid;
    int a, j;							    int a, j;
    cpid = fork();						    cpid = fork();
    if (cpid == 0)						    if (cpid == 0)
    {								    {
        a = a0;							        a = a0;
        for(j = 0; j < k; j++)					        for(j = 0; j < k; j++)
        {							        {
            lseek(h, (i + j * n) * sizeof(int), SEEK_SET);	            lseek(h, (i + j * n) * sizeof(int), SEEK_SET);
            write(h, &a, sizeof(int));				            write(h, &a, sizeof(int));
            a += d;						            a += d;
        }							        }
        _exit(EXIT_SUCCESS);					        _exit(EXIT_SUCCESS);
    } else							    } else
        return(cpid);						        return(cpid);
}								}

int main(int argc, char *argv[])				int main(int argc, char *argv[])
{								{
    int n, a0, d, k, i;						    int n, a0, d, k, i;
    int h;							    int h;
    char f[132];						    char f[132];
    pid_t *cpids;						    pid_t *cpids;
							      >	    int pstat;

    if (argc == 1)						    if (argc == 1)
    {								    {
        n = 3;							        n = 3;
        strcpy(f, "out.bin");					        strcpy(f, "out.bin");
        a0 = 4;							        a0 = 4;
        d = 2;							        d = 2;
        k = 4;							        k = 4;
    } else if (argc == 6) {					    } else if (argc == 6) {
        if(sscanf(argv[1], "%d", &n) != 1)			        if(sscanf(argv[1], "%d", &n) != 1)
            exit(1);						            exit(1);
        if(sscanf(argv[2], "%s", f) != 1)			        if(sscanf(argv[2], "%s", f) != 1)
            exit(1);						            exit(1);
        if(sscanf(argv[3], "%d", &a0) != 1)			        if(sscanf(argv[3], "%d", &a0) != 1)
            exit(1);						            exit(1);
        if(sscanf(argv[4], "%d", &d) != 1)			        if(sscanf(argv[4], "%d", &d) != 1)
            exit(1);						            exit(1);
        if(sscanf(argv[5], "%d", &k) != 1)			        if(sscanf(argv[5], "%d", &k) != 1)
            exit(1);						            exit(1);
    } else {							    } else {
        fprintf(stderr, "%s: 3 out.bin 4 2 4", argv[0]);	        fprintf(stderr, "%s: 3 out.bin 4 2 4", argv[0]);
        exit(1);						        exit(1);
    }								    }
#if DEBUG							#if DEBUG
    printf("N=%d F=%s A0=%d D=%d K=%d\n", n, f, a0, d, k);	    printf("N=%d F=%s A0=%d D=%d K=%d\n", n, f, a0, d, k);
#endif								#endif
    h = creat(f, S_IRUSR|S_IWUSR| S_IRGRP| S_IROTH);		    h = creat(f, S_IRUSR|S_IWUSR| S_IRGRP| S_IROTH);
							      >	    ftruncate(h, n * k * sizeof(int));

    cpids = malloc(n * sizeof(pid_t));				    cpids = malloc(n * sizeof(pid_t));
    if(cpids == NULL)						    if(cpids == NULL)
        return -1;						        return -1;

    for(i = 0; i < n; i++)					    for(i = 0; i < n; i++)
        cpids[i] = startArithmeticProgression(h, i, n, a0 + d	        cpids[i] = startArithmeticProgression(h, i, n, a0 + d

    wait(NULL);						      |	    //wait(NULL);
							      >	    for(i = 0; i < n; i++)
							      >	    {
							      >	        waitpid(cpids[i], &pstat, 0);
							      >	        /*if (WIFEXITED(pstat))
							      >	        {
							      >	            exitstatus = WEXITSTATUS(pstat);
							      >	            if (exitstatus)
							      >	            {
							      >	                if (mypid==getpid())
							      >	                {
							      >	                    printf("-1\n");
							      >	                    exit(EXIT_SUCCESS);
							      >	                }
							      >	                exit(EXIT_FAILURE);
							      >	            }
							      >	        }*/
							      >	    }

    free(cpids);						    free(cpids);

    close(h);							    close(h);

    return 0;							    return 0;
}								}

// http://www.opennet.ru/base/dev/unix_signals.txt.html		// http://www.opennet.ru/base/dev/unix_signals.txt.html
